@model List<Azimzadeh_MVC_project.Models.Gallery>
@{
    ViewBag.Title = "مدیریت گالری تصاویر";
}

<div class="row">
    <div class="col-lg-12">
        <section class="panel">
            <header class="panel-heading">
                <i class="icon-picture"></i> مدیریت گالری تصاویر
                <a href="@Url.Action("Create", "Gallery", new { area = "Admin" })" class="btn btn-success btn-sm pull-left" style="margin-top: -4px;">
                    <i class="icon-plus"></i> افزودن تصویر جدید
                </a>
            </header>
            <div class="panel-body">
                @if (Model == null || Model.Count == 0)
                {
                    <div class="alert alert-info text-center">هیچ تصویری در گالری یافت نشد. برای شروع دکمه «افزودن تصویر جدید» را کلیک کنید.</div>
                }
                else
                {
                    <table class="table table-striped table-hover table-advance">
                        <thead>
                            <tr>
                                <th style="width: 8%;">#</th>
                                <th style="width: 15%;">تصویر</th>
                                <th>عنوان</th>
                                <th>توضیحات</th>
                                <th style="width: 12%; text-align: center;">ترتیب نمایش</th>
                                <th style="width: 10%;">وضعیت</th>
                                <th style="width: 15%;">عملیات</th>
                            </tr>
                        </thead>
                        <tbody>
                            @{int rowNum = 1;}
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@rowNum</td>
                                    <td>
                                        <img src="@item.ImagePath" alt="@item.Title" style="max-height: 50px; max-width: 120px; border-radius: 4px; border: 1px solid #ddd;" />
                                    </td>
                                    <td><strong>@item.Title</strong></td>
                                    <td>@item.Description</td>
                                    <td style="text-align: center;">@item.DisplayOrder</td>
                                    <td>
                                        @if (item.IsActive)
                                        {
                                            <span class="label label-success label-mini">فعال</span>
                                        }
                                        else
                                        {
                                            <span class="label label-danger label-mini">غیرفعال</span>
                                        }
                                    </td>
                                    <td>
                                        <a href="@Url.Action("Edit", "Gallery", new { area = "Admin", id = item.GalleryId })" class="btn btn-primary btn-xs" title="ویرایش">
                                            <i class="icon-pencil"></i>
                                        </a>

                                        @using (Html.BeginForm("ToggleActive", "Gallery", new { area = "Admin", id = item.GalleryId }, FormMethod.Post, new { style = "display:inline;" }))
                                        {
                                            @Html.AntiForgeryToken()
                                            if (item.IsActive)
                                            {
                                                <button type="submit" class="btn btn-warning btn-xs" title="غیرفعال کردن">
                                                    <i class="icon-eye-close"></i>
                                                </button>
                                            }
                                            else
                                            {
                                                <button type="submit" class="btn btn-success btn-xs" title="فعال کردن">
                                                    <i class="icon-eye-open"></i>
                                                </button>
                                            }
                                        }

                                        @using (Html.BeginForm("Delete", "Gallery", new { area = "Admin", id = item.GalleryId }, FormMethod.Post, new { style = "display:inline;", onsubmit = "return confirm('آیا از حذف این تصویر مطمئن هستید؟');" }))
                                        {
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="btn btn-danger btn-xs" title="حذف">
                                                <i class="icon-trash"></i>
                                            </button>
                                        }
                                    </td>
                                </tr>
                                rowNum++;
                            }
                        </tbody>
                    </table>
                }
            </div>
        </section>
    </div>
</div>
