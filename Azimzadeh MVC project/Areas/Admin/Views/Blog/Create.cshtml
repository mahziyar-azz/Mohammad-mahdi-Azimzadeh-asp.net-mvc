@model Azimzadeh_MVC_project.Models.BlogPost
@{
    ViewBag.Title = "افزودن مقاله جدید";
}

<div class="row">
    <div class="col-lg-10 col-lg-offset-1">
        <section class="panel">
            <header class="panel-heading">
                <i class="icon-plus"></i> افزودن مقاله جدید به وبلاگ
            </header>
            
            <div class="panel-body">
                @using (Html.BeginForm("Create", "Blog", FormMethod.Post, new { @class = "form-horizontal tasi-form", enctype = "multipart/form-data" }))
                {
                    @Html.AntiForgeryToken()
                    @Html.ValidationSummary(true, "", new { @class = "text-danger alert alert-danger" })

                    <div class="form-group">
                        @Html.LabelFor(model => model.Title, "عنوان مقاله *", new { @class = "col-lg-2 control-label" })
                        <div class="col-lg-10">
                            @Html.TextBoxFor(model => model.Title, new { @class = "form-control", placeholder = "عنوان مقاله را وارد کنید", required = "required" })
                            @Html.ValidationMessageFor(model => model.Title, "", new { @class = "text-danger" })
                        </div>
                    </div>

                    <div class="form-group">
                        @Html.LabelFor(model => model.BlogCategoryId, "دسته‌بندی مقاله", new { @class = "col-lg-2 control-label" })
                        <div class="col-lg-5">
                            @Html.DropDownList("BlogCategoryId", null, "انتخاب دسته‌بندی...", new { @class = "form-control" })
                            @Html.ValidationMessageFor(model => model.BlogCategoryId, "", new { @class = "text-danger" })
                        </div>

                        @Html.LabelFor(model => model.IsPublished, "وضعیت انتشار", new { @class = "col-lg-2 control-label" })
                        <div class="col-lg-3">
                            <div class="checkbox">
                                <label>
                                    @Html.CheckBoxFor(model => model.IsPublished) منتشر شود
                                </label>
                            </div>
                        </div>
                    </div>

                    <div class="form-group">
                        @Html.LabelFor(model => model.ShortDescription, "خلاصه مقاله (توضیح کوتاه)", new { @class = "col-lg-2 control-label" })
                        <div class="col-lg-10">
                            @Html.TextAreaFor(model => model.ShortDescription, new { @class = "form-control", rows = "2", placeholder = "توضیح مختصری که در نتایج سرچ و صفحات آرشیو وبلاگ نمایش داده می‌شود" })
                            @Html.ValidationMessageFor(model => model.ShortDescription, "", new { @class = "text-danger" })
                        </div>
                    </div>

                    <div class="form-group">
                        @Html.LabelFor(model => model.Content, "محتوای مقاله (HTML) *", new { @class = "col-lg-2 control-label" })
                        <div class="col-lg-10">
                            @Html.TextAreaFor(model => model.Content, new { @class = "form-control", rows = "12", placeholder = "محتوای کامل مقاله شامل تگ‌های HTML در صورت لزوم", required = "required" })
                            @Html.ValidationMessageFor(model => model.Content, "", new { @class = "text-danger" })
                        </div>
                    </div>

                    <div class="form-group">
                        <label class="col-lg-2 control-label">تصویر شاخص مقاله</label>
                        <div class="col-lg-10">
                            <input type="file" name="imageFile" class="form-control" accept="image/*" />
                            <span class="help-block text-muted">فرمت‌های مجاز: jpg, jpeg, png.</span>
                        </div>
                    </div>

                    <div class="form-group">
                        <div class="col-lg-offset-2 col-lg-10">
                            <button type="submit" class="btn btn-success"><i class="icon-save"></i> ثبت و ذخیره مقاله</button>
                            <a href="@Url.Action("Index", "Blog", new { area = "Admin" })" class="btn btn-default">انصراف</a>
                        </div>
                    </div>
                }
            </div>
        </section>
    </div>
</div>
